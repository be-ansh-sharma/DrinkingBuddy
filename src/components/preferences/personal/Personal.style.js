import { StyleSheet } from 'react-native';
import { COLOR } from 'global/styles';

export default StyleSheet.create({
  container: {
  },
  subHeader: {
    color: COLOR.primary,
  },
  text: {
    color: COLOR.accent,
    alignSelf: 'center',
    //textTransform: 'ca',
  },
});

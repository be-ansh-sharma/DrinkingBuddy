import { StyleSheet } from 'react-native';
import { COLOR } from 'global/styles';

export default StyleSheet.create({
  subHeader: {
    color: COLOR.primary,
  },
  icon: {
    alignSelf: 'center',
  },
});

import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  picker: {
    flexDirection: 'row',
    justifyContent: 'center',
  },
  pickerItem: {
    alignSelf: 'center',
    marginLeft: 20,
    marginBottom: 8,
  },
});

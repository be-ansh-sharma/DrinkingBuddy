import { StyleSheet } from 'react-native';
import { COLOR } from '../../global/styles';

export default StyleSheet.create({
  heading: {
    marginTop: '20%',
    alignSelf: 'center',
    fontSize: 24,
    color: COLOR.primary,
  },
});

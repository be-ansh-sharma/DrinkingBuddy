import { StyleSheet } from 'react-native';
import { COLOR } from '../../../global/styles';

export default StyleSheet.create({
  container: {
    borderWidth: 1,
  },
  subHeader: {
    color: COLOR.primary,
  },
});
